# Session Patterns

Recurring behaviors detected across sessions. Auto-populated by auto-scribe.
Surfaced by memory-whisper when relevant. Max 20 entries.

## Patterns

(empty — will be populated after first auto-scribe run)
