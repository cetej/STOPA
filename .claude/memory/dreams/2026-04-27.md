---
date: 2026-04-27
learnings_scanned: 4
outcomes_scanned: 0
connections_found: 3
backward_updates: 1
patterns_detected: 1
duration_estimate: 3min
cycle_type: abbreviated
---

# Dream Log — 2026-04-27

## Trigger

1 new learning since last dream (2026-04-26): `2026-04-26-autoreason-mid-tier-sweet-spot.md` (medium severity, best_practice, skill component, source: external_research). Written ~12h after the previous dream cycle ran (mtime delta: dream 1777186941 vs learning 1777232478). 0 new outcomes. Smart Skip declined because new learning exists; abbreviated cycle (Phase 2c–2d skipped per rule "<3 new items").

## New Connections

The new mid-tier learning landed with only `related: [claudini-autoresearch-loop]` despite richer ties to existing autoreason/iterative-loop cluster. Three additional cross-links added:

- `2026-04-26-autoreason-mid-tier-sweet-spot.md` ↔ `2026-04-01-autoreason-adversarial-debate.md` (NEW): Architecture doc (April 1) defined the AutoReason pattern from literature with `confidence: 0.6`. The April 26 NousResearch paper provides empirical validation of the same pattern (Haiku 3.5 + structure → 42/42, mid-tier recovery 62 % vs 43 %). Backward link added to architecture doc; confidence raised 0.6 → 0.75 (additive — same meaning, now empirically grounded). The pair forms the complete autoreason story: WHY the structure works (cold-start isolation, blind judges, no consensus collapse) ↔ WHAT it produces empirically (mid-tier recovery, frontier-level output at 10× lower cost).

- `2026-04-26-autoreason-mid-tier-sweet-spot.md` ↔ `karpathy-loop-autoloop.md` (NEW): Karpathy's heuristic "structural fast loop + LLM final judge > LLM-judge-everywhere" parallels the new finding "structure (A/B/AB + blind judges + Borda) > model upgrade". Same anti-pattern critique: brute force (large model OR LLM-judge per iteration) loses to disciplined workflow design. Karpathy targets cost; AutoReason targets confirmation-bias collapse — both arrive at "structure beats raw capability" via different angles.

- `2026-04-26-autoreason-mid-tier-sweet-spot.md` ↔ `2026-03-29-claudini-autoresearch-loop.md` (PRESERVED): Already linked. Both prescribe agent loops; mid-tier sweet spot complements claudini's "start from existing implementation > invent from scratch" — both express "process design > raw resource investment".

All links bidirectional. New learning now at `related: [autoreason-adversarial-debate, claudini-autoresearch-loop, karpathy-loop-autoloop]` (3/3 capacity). Architecture doc now at 2/3 capacity.

## Backward Updates

- `2026-04-01-autoreason-adversarial-debate.md`: Added `2026-04-26-autoreason-mid-tier-sweet-spot.md` to `related:` array. Bumped `confidence: 0.6 → 0.75`. Justification: the April 1 doc was literature-derived (PoLL, Wynn et al., Chatbot Arena) without empirical STOPA-context validation. The April 26 paper provides direct empirical confirmation of three of its core claims (cold-start isolation, blind judges > single large judge, structured critique > consensus). Per "do not change meaning" rule: text untouched, only frontmatter updated to reflect strengthened evidence base. Confidence boost is additive, not narrative-altering.

## Patterns Detected

**"Structure > raw capability" meta-pattern emerging across iterative-skill cluster.** Three independent learnings now converge on this principle from different vantage points:

1. `karpathy-loop-autoloop` (March 23): structural fast-loop + single final LLM-judge beats LLM-judge-every-iteration on cost AND quality
2. `claudini-autoresearch-loop` (March 29): starting from existing implementation + dense scalar feedback beats raw model capability on red-teaming SOTA
3. `autoreason-mid-tier-sweet-spot` (April 26): A/B/AB triáda + blind judges + Borda beats Opus-on-writers; Haiku 3.5 + structure = 42/42

Common thread: **the iterative-skill domain consistently rewards workflow design over resource investment**. Frontier model cost can be saved by mid-tier model + better loop structure. This is a graduation candidate for `critical-patterns.md` after one more independent supporting datapoint (currently 3 — graduation rule needs ≥3 independent + cross-skill applicability, which is satisfied; but all three are `external_research` source rather than `auto_pattern` from STOPA traces). Recommendation: flag for `/evolve` review next cycle, but do not auto-graduate — wait for one STOPA-native validation (e.g., autoresearch outcome trace where structure-vs-model A/B was actually run).

## Pending Clusters (carried forward)

- 27 dedup candidates from autodream report (yesterday) — still all at jaccard < 0.15. No action.
- Confirmation-bias / autoresearch quality triangle — pending Falsification Step in `/autoresearch` SKILL.md. Out of dreams scope.
- MCP-config cluster (yesterday) — `mcp-config-canonical-location` flagged for `/evolve` graduation review; `verify_check` reportedly failing per session-start hook. Carried forward.

## Replay Queue

Empty. No HERA-style validation pending.

## Consolidation Health

- Total learnings: ~188 (one new since 2026-04-26)
- New cross-link pairs this cycle: 3 (2 newly added, 1 preserved)
- Backward updates: 1 (architecture doc confidence + related)
- Graduation candidates: 1 meta-pattern flagged ("Structure > raw capability"), pending one STOPA-native validation
- Archive: 0 (no learnings hit decay floor)

## Suggested Actions

- Next `/evolve` cycle: review the "Structure > raw capability" meta-pattern across the three iterative-skill learnings; consider drafting a `critical-patterns.md` entry that cross-references all three rather than picking one for graduation.
- Carry forward MCP-config `verify_check` issue from yesterday's dream — still unresolved per autonomy log.
- No action required from user; abbreviated cycle complete.
