---
date: 2026-04-16
learnings_scanned: 5
outcomes_scanned: 1
connections_found: 5
backward_updates: 2
patterns_detected: 1
duration_estimate: 4min
---

# Dream Log — 2026-04-16

## New Connections

| Learning A | Learning B | Link | Reason |
|-----------|-----------|------|--------|
| prm-step-verification-orchestrate | sd-zero-self-revision-supervision | composes | Two-layer lightweight quality gate: SD-ZERO self-revision runs first (free, no agent spawn), PRM step-check runs second (haiku, ~200 tokens). Together they catch most errors before expensive full critic. |
| parcae-exponential-decay-stopping | iteration-paradox-meta-pattern | refines | Parcae's exponential model L(T)=L∞+Z·exp(-z·T) is more accurate than sigmoidal (3 params vs 4, fits after 3-4 points). Updates Step 4 of the Iteration Paradox protocol. |
| parcae-exponential-decay-stopping | best-of-n-parallel-candidates | orthogonal | Parcae = depth scaling (when to stop iterating a single candidate). Best-of-N = breadth scaling (how many candidates to explore in parallel). Together they cover both axes of test-time compute. |
| prm-step-verification-orchestrate | experience-replay-outcomes-reuse | enriches | PRM step-level credit assignment gives replay finer-grained labels — which step failed, not just run-level outcome. Future enhancement: annotate step_results[] in outcomes for step-level replay. |
| sd-zero-self-revision-supervision | generate-then-discard-suboptimal | mechanism | SD-ZERO provides the mechanism that makes generate-then-discard actively useful: failed outputs become dense token-level supervision via self-revision rather than being discarded silently. |

## Backward Updates

1. **`iteration-paradox-meta-pattern` (2026-04-11)**: Added context that Parcae (arXiv:2604.12946) provides a better ceiling estimator than sigmoidal — exponential fit L(T)=L∞+Z·exp(-z·T), fits after 3-4 iterations. Step 4 of the protocol should prefer exponential over sigmoidal when ≥4 data points are available.

2. **`experience-replay-outcomes-reuse` (2026-04-13)**: Added context that PRM step-verification can enrich outcome replay labels. Current outcomes record run-level signal; future enhancement: annotate which step failed via step_results[] for step-level attribution.

## Emerging Patterns

### Pattern: "Test-Time Compute Stack" (5 learnings, 3 dates)

A coherent cluster covering how to efficiently scale inference-time compute across different dimensions:

Evidence (cross-session, maturity-eligible):
- `parcae-exponential-decay-stopping` (2026-04-15): exponential decay ceiling model — when to stop
- `best-of-n-parallel-candidates` (2026-04-15): parallel breadth exploration — how many candidates
- `prm-step-verification-orchestrate` (2026-04-15): step-level quality gating — where errors occur
- `sd-zero-self-revision-supervision` (2026-04-15): self-revision before external evaluation — cheapest first
- `iteration-paradox-meta-pattern` (2026-04-11): meta-resolution of iterative optimization tradeoffs

**Combined execution order for iterative skills (deep tier):**
1. Generate N=2-3 parallel candidates (Best-of-N breadth)
2. Each candidate self-revises with SD-ZERO pattern before critic
3. Lightweight critic scores candidates → pick best
4. Iterate on winner with step-level PRM checks after each subtask
5. Fit L(T)=L∞+Z·exp(-z·T) after 4+ iterations → predict ceiling → stop early if L∞ < target

This is 3-session validated (Apr 11 + Apr 13 + Apr 15). Cross-component (skill + orchestration + general).

---

## Replay Queue Status

Empty — no pending failure-sourced learnings. No action needed.

---

## Consolidation Health

- Total learnings: ~158
- New learnings since last dream: 5
- Cross-links added: 5 (bidirectional across 5 files)
- Backward updates: 2
- Replay queue: empty
- Graduation candidates: 0 (new learnings uses < 5; msys-tmp-path-mismatch = high confidence but uses=0)
- critical-patterns.md: full at 10/10 (requires demotion before next graduation)
- Concept graph: missing (Phase 2d skipped)

## Suggested Actions

1. **"Test-Time Compute Stack" → candidate for CLAUDE.md note**: The 5-learning cluster has a clear combined protocol (self-revision → Best-of-N → PRM step-check → exponential stopping). Once individual learnings accumulate uses, candidate for behavioral-genome.md under Orchestration section.

2. **Farm tier enhancement (Parcae)**: Per-item variable depth for farm tier is actionable. Orchestrate Phase 3 (planning) could classify item complexity (simple/complex) before distributing to workers. Currently deferred; should surface at next orchestrate SKILL.md edit.

3. **PRM implementation check**: `prm-step-verification-orchestrate` has uses=1 but its proposal hasn't been implemented in orchestrate/SKILL.md. Worth verifying at /evolve whether the step-level check was actually added or remains a proposal.
