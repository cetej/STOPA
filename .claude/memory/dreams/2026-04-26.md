---
date: 2026-04-26
learnings_scanned: 3
outcomes_scanned: 0
connections_found: 4
backward_updates: 0
patterns_detected: 0
duration_estimate: 4min
cycle_type: abbreviated
---

# Dream Log — 2026-04-26

## Trigger

1 new learning since last real dream (2026-04-24): `2026-04-25-mcp-config-canonical-location.md` (high severity, bug_fix, hook component). 0 new outcomes, 0 strong merge candidates (all autodream dedup pairs have jaccard < 0.15), replay queue empty. Smart Skip declined because new learning exists; abbreviated cycle (Phase 2c–2d skipped per rule "<3 new items").

## New Connections

MCP-config cluster created — three previously isolated MCP-related learnings now cross-linked through the new canonical-location finding:

- `2026-04-25-mcp-config-canonical-location.md` ↔ `2026-03-27-secrets-in-config-files.md`: New learning explicitly invokes the "no secrets in JSON" rule when documenting `claude mcp add-json` ("NEVER put API keys in this JSON"). Pair forms the complete config-hygiene story: WHERE to register MCP servers (canonical-location) and WHAT must never go in those configs (secrets-in-config).
- `2026-04-25-mcp-config-canonical-location.md` ↔ `2026-03-27-playwright-mcp-download-hijack.md`: Playwright incident required cleanup across `~/.claude/settings.json`, `claude_desktop_config.json`, and `settings.local.json` permissions — the new learning clarifies that the `mcpServers` block in `settings.local.json` was never actually loaded by CC, only the `permissions` block was. Together they bound the surface area: canonical config location vs. legacy/orphan files that may still contain stale entries. Removal procedure for any rogue MCP must touch BOTH places.
- `2026-04-25-mcp-config-canonical-location.md` ↔ `2026-04-14-mcp-server-token-overhead.md`: Token-overhead learning prescribes "audit MCP connections at session start"; canonical-location learning provides the operational answer ("audit via `claude mcp list`, edit via `claude mcp add-json`"). Operational pair — one says WHY to audit, the other says HOW.
- Triangle now closed: `secrets-in-config` ↔ `playwright-mcp-hijack` link added (both 2026-03-27 incidents touched the same Claude Desktop config; mutually reinforcing security finding for the same surface).

All links bidirectional. New learning sits at center with `related: [secrets-in-config, playwright-mcp-hijack, mcp-server-token-overhead]` (3/3 capacity). `mcp-server-token-overhead` now at 3/3 capacity.

## Backward Updates

None this cycle. Considered appending a note to `2026-03-27-playwright-mcp-download-hijack.md` clarifying that `settings.local.json` `mcpServers` block was never loaded — but at the time the playwright incident played out, removal of that block was still part of the correct remediation path (defense in depth). Backward-update would muddy the historical record. Skipped per "do not change meaning of old learnings" rule.

## Pending Clusters (carried forward)

- 27 dedup candidates from autodream report — all at jaccard < 0.15, none meet 0.5 merge threshold. Tag overlap is genuine but summaries diverge enough to keep separate. No action.
- Confirmation-bias / autoresearch quality triangle (from 2026-04-24 dream) — still pending action item to add Falsification Step to `/autoresearch` SKILL.md. Out of scope for dreams.

## Replay Queue

Empty. No HERA-style validation pending.

## Consolidation Health

- Total learnings: 187 (autodream scan)
- New cross-link pairs this cycle: 4 (all bidirectional)
- Backward updates: 0
- Graduation candidates: 0 (autodream report)
- Maintained confidence boosts (autodream): 4 files (claudini-autoresearch-loop → 1.0, small-model-compression → 0.86, cc-graduated-compaction → 0.95, parallel-monitoring-failure → 1.0)
- Archive: 0 (no learnings hit decay floor)

## Suggested Actions

- New `mcp-config-canonical-location` learning is `maturity: validated` with `confidence: 1.0` and high `uses` — already at graduation threshold. Trigger `/evolve` for promotion review (component: hook → could graduate to `critical-patterns.md` since `skill_scope: []`).
- `verify_check` on canonical-location learning currently fails (Grep `mcpServers` in `~/.claude.json` → 0 matches per session-start violation report). Either the user's `~/.claude.json` was modified, or path needs adjustment. Out of scope for dreams (consolidation-only); flag for `/evolve` or manual review.
