---
date: 2026-04-17
learnings_scanned: 1
outcomes_scanned: 0
connections_found: 3
backward_updates: 1
patterns_detected: 1
duration_estimate: 3min
---

# Dream Log — 2026-04-17

## New Connections

| Learning A | Learning B | Link | Reason |
|-----------|-----------|------|--------|
| hook-cwd-anchor-pattern (2026-04-16) | msys-tmp-path-mismatch (2026-04-15) | same-cluster | Both Windows path resolution failures in bash hooks — MSYS /tmp translation vs CWD dependency. Same failure pattern (silent write to wrong location), different trigger. |
| hook-cwd-anchor-pattern (2026-04-16) | hook-failure-modes (2026-04-07) | extends | CWD dependency is an unlisted 6th failure mode. Added backward-update note. |
| hook-cwd-anchor-pattern (2026-04-16) | hook-testing-patterns (2026-04-07) | informs | Fix requires testing from multiple CWDs — aligns with multi-context testing guidance. |

All links bidirectional.

## Backward Updates

1. **`hook-failure-modes` (2026-04-07)**: Added failure mode #6 "CWD Dependency (Silent Path Corruption)" with mitigation pattern from `hook-cwd-anchor-pattern`. Cross-reference link included.

## Emerging Patterns

### Pattern: "Windows Hook Path Failures" (3 learnings, 3 dates)

Cross-session validated cluster of Windows-specific path handling failures in bash/Python hooks:

- `hook-failure-modes` (2026-04-07): Windows section already mentioned path separators and file locking
- `msys-tmp-path-mismatch` (2026-04-15): MSYS2 translates /tmp before Python sees it → wrong directory
- `hook-cwd-anchor-pattern` (2026-04-16): relative paths use CWD at invocation → writes to wrong location

**Common thread**: Path resolution in hooks is fragile on Windows because bash, MSYS2, and Python each apply different resolution rules. Hooks must externalize all path resolution to explicit anchoring.

**3 dates, 3 sessions → maturity-eligible** for `msys-tmp-path-mismatch` and `hook-cwd-anchor-pattern` (both `draft`). Suggest `/evolve` review both for upgrade to `validated`.

---

## Replay Queue Status

Empty — no pending failure-sourced learnings.

---

## Consolidation Health

- Total learnings: ~154 (directory count)
- New learnings since last dream: 1
- Cross-links added: 3 (bidirectional across 4 files)
- Backward updates: 1 (hook-failure-modes, section appended)
- Replay queue: empty
- Patterns detected: 1 ("Windows Hook Path Failures" — 3-session validated)
- Graduation candidates: 0 this cycle (new learning uses=1, pattern learnings uses=3 and 1)
- Smart Skip: not triggered (1 new learning present)

## Suggested Actions

1. **Maturity upgrade for `msys-tmp-path-mismatch`**: uses=3, harmful_uses=0, source=user_correction (high trust). Pattern now 3-session validated. Candidate for `maturity: validated` at next `/evolve`.

2. **Hook audit reminder**: `hook-cwd-anchor-pattern` notes 25+ other hooks should be audited for same relative-path bug. `verify_check` covers one specific file but not the broader audit. Consider a one-time sweep of all `.claude/hooks/*.sh` for missing SCRIPT_DIR anchoring.

3. **"Test-Time Compute Stack" pattern from 2026-04-16**: still no uses on constituent learnings. Monitor at next dreams — if uses remain 0 by 2026-05-17, consider decay warning.
