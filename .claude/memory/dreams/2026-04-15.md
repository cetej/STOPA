---
date: 2026-04-15
learnings_scanned: 7
outcomes_scanned: 1
connections_found: 8
backward_updates: 2
patterns_detected: 2
duration_estimate: 6min
---

# Dream Log — 2026-04-15

## New Connections

| Learning A | Learning B | Link | Reason |
|-----------|-----------|------|--------|
| dacs-context-scoping | task-guided-context-beats-raw-sharing | implements | DACS Registry/Focus mode is the concrete implementation of the task-guided context selection principle; both address the same multi-agent context contamination problem |
| dacs-context-scoping | compression-regime-maps-to-tiers | complements | DACS (per-agent switching) + compression-regime (per-tier strategy) = complete multi-agent context management stack |
| traceguard-5d-critic | judge-panel-size-convergence | complements | Dimensional decomposition (5D) + panel size per dimension (3-7 judges) = complete evaluation architecture; TraceGuard provides the axes, judge-panel provides the convergence model |
| compact-timing-60pct | autocompact-threshold | same_problem | Both address proactive compaction: env var 70% (automatic safety net) + manual 60% rule (proactive human trigger). Consistent targets. |
| compact-timing-60pct | prompt-cache-ttl-5min | paired_protocol | Together they form the session token management protocol: compact at 60% during work; compact/clear before breaks to avoid 5min cache miss |
| mcp-server-token-overhead | purpose-built-tools-75x-faster | reinforces | Both argue for lighter tools: MCP = 18K tokens/message hidden overhead; generic MCP = 75× slower than narrow CLI. Same principle, two angles (cost and speed) |
| mcp-server-token-overhead | compact-timing-60pct | same_cluster | Both belong to the "Token Hygiene Protocol" pattern cluster |
| paseo-orchestration-patterns | morning-briefing-cron-pattern | same_pattern | Both use CronCreate-based scheduling for autonomous periodic orchestration (Paseo heartbeat + morning briefing digest share the same implementation primitive) |

## Backward Updates

1. **`autocompact-threshold` (2026-04-01)**: Added context note that the independent vlog analysis (2026-04-14) recommends manual /compact at 60% — slightly more conservative than the 70% env var. Updated mental model: 70% = autocompact safety net, 60% = proactive threshold. After 3-4 compacts, switch to /clear + fresh start.

2. **`task-guided-context-beats-raw-sharing` (2026-04-11)**: Added context note about DACS providing the empirical baseline — without selective context, steering accuracy drops to 21-60%; DACS Registry/Focus achieves 90-98%. DACS is now the implementation reference for >3 concurrent agents.

## Emerging Patterns

### Pattern: "Token Hygiene Protocol" (5 learnings across 3 dates)

A cluster of token management rules that collectively form a session-start protocol:

Evidence:
- `autocompact-threshold` (2026-04-01): env var 70% safety net
- `purpose-built-tools-75x-faster` (2026-04-12): narrow tools 75× faster than generic MCP
- `compact-timing-60pct` (2026-04-14): manual compact at 60%
- `prompt-cache-ttl-5min` (2026-04-14): 5min TTL = compact before breaks
- `mcp-server-token-overhead` (2026-04-14): 18K tokens per connected MCP server

**Combined session-start protocol:**
1. Audit MCP connections — disconnect servers not needed for this task
2. Set CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=70 (already in settings.json)
3. During session: /compact manually when reaching ~60%
4. Before stepping away >5 min: /compact or /checkpoint + /clear
5. After 3-4 compacts: /clear + fresh start with checkpoint resume

This is cross-session validated (April 1 + April 12 + April 14), maturity-eligible as cluster once individual learnings accumulate uses.

### Pattern: "Multi-Agent Context Management Spectrum" (3 learnings)

Three complementary pieces that together define how to manage context across orchestrated agents:

Evidence:
- `compression-regime-maps-to-tiers` (2026-04-11): what compression level per task complexity
- `task-guided-context-beats-raw-sharing` (2026-04-11): what content to include per worker (task-relevant, not raw dump)
- `dacs-context-scoping` (2026-04-14): how to switch attention between agents (Registry/Focus mode)

**Combined protocol for orchestrate Phase 4:**
- Determine tier (light/standard/deep/farm) → pick compression regime
- Build per-subtask context packet (relevant scout facts, exclude orchestrator reasoning)
- If >3 concurrent agents: apply DACS Registry mode (≤200 tokens/agent summary) with Focus switch on NEEDS_CONTEXT

---

## Replay Queue Status

Queue is empty — no pending failure-sourced learnings. No action needed.

---

## Consolidation Health

- Total learnings: ~155 (148 from autodream + 7 new)
- New learnings since last dream: 7
- Cross-links added: 8 (across 8 files)
- Backward updates: 2
- Replay queue: empty
- Graduation candidates: 0 (all new learnings uses < 5)
- critical-patterns.md: full at 10/10 (next graduation requires demotion)
- Concept graph: missing (no Phase 2d executed)

## Suggested Actions

1. **Token Hygiene Protocol → candidate for CLAUDE.md note**: The 5-learning cluster is actionable as a session-start checklist. Could be added to behavioral-genome.md under a new "Session Start" section after a few learnings accumulate uses.

2. **critical-patterns.md capacity**: Full at 10/10. Pattern #8 (Sonnet 4.6 thinking/effort breaking change) has a `model_gate: sonnet-4.6` annotation — if Sonnet 5.x is in use, this should be reviewed for demotion. Defer to /evolve.

3. **DACS implementation gap**: The `verify_check` on `dacs-context-scoping` asserts DACS appears in orchestrate/SKILL.md. The learning says "added to SKILL.md" but this hasn't been verified independently. Worth checking at next /evolve or orchestrate edit.
