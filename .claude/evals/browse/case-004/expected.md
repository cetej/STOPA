# Expected Behavior

The skill file SHOULD contain all required sections per skill-files.md rules:
- `## Anti-Rationalization Defense` section with a 3-column table
- `## Red Flags` section with observable symptoms
- `## Verification Checklist` section with exit criteria
- `permission-tier:` field in YAML frontmatter
- `discovery-keywords:` field in YAML frontmatter

Currently these sections are ABSENT from browse/SKILL.md — this case FAILS baseline.
